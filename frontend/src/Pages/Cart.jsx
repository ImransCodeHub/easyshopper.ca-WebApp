import React from 'react';
import PropTypes from 'prop-types';

const Cart = () => {
    return (
        <div>
            {/* cart component content */}
        </div>
    );
};

export default Cart;